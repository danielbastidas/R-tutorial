<html>

<head>
<title>Title</title>
</head>

<body>

<p>This is an R HTML document. When you click the <b>Knit HTML</b> button a web page will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:</p>

<!--begin.rcode
library(ggplot2)
ggplot(diamonds, aes(x=color)) + geom_bar()
end.rcode-->

<p>Look mom I'm including a named chunk in order to facilitate the debugging process:</p>

<!--begin.rcode fig.width=7, fig.height=6
ggplot(diamonds, aes(x=color, fill=color)) + geom_bar()
end.rcode-->

<p>Inline examples:</p>

<ul>
  <li>1 + 3 = <!-- rinline 1 + 3  --></li>
  <li>&pi; = <!-- rinline pi  --></li>
</ul>

<p>A document similar to this one can be generated too using R Markdown</p>

</body>
</html>
